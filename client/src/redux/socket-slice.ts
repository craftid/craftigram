// client/src/redux/socket-slice.ts
import { createSlice } from "@reduxjs/toolkit"

const socketSlice = createSlice({
	name: "socketio",
	initialState: {},
	reducers: {},
})

export default socketSlice.reducer
